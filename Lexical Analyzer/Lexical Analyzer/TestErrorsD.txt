program example(input, output);
var x:integer; var y: integer; var z: integer;
function gcd(a:integer; b: integer): real;
begin {comment test}
	if b + 0 then gcd := a
	else gcd := gcd(b, a mod b)
end;
function gcd2(a:real; b: real): real;
begin {comment test}
	while b = 0 do
	   gcd2 := gcd2(b, a mod b)
end;
begin
	z := gcd(x, y);
	z := z and x;
	z := z < x;
	z := y > x;
	z := x <> y;
	z := not x;
	z := x >= y;
	z := x <= y;
	z := x or y
end.
